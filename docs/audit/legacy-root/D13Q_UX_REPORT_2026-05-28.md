# D.13Q-UX Closure — 完成报告（中文）

**日期**：2026-05-28（续 2026-05-29）
**状态**：本地完成（typecheck / build / 关键单测 / git diff --check 全绿；未提交）
**交付文档**：`docs/delivery/phase-13Q-ux.md`
**前置版本**：上一轮（同日 8:08）留下 5 个 micro-pass 后掉线；本轮恢复现场后把全部 micro-pass 闭合。

---

## 恢复现场后发现的工作树状态

进入会话时 `git status --short` 已经显示：

- **13 个 modified 文件**：`packages/tui/src/index.{ts,test.ts}`、`packages/tui/src/model-loop-runtime.{ts,test.ts}`、`packages/tui/src/shell/{components/{Composer,ProductBlock,ShellApp}.tsx,plain-renderer.ts,theme.ts,types.ts,view-model.ts,view-model.test.ts}`、`packages/tui/src/tui-permission-runtime.ts`
- **9 个 untracked 组件 / model**：`BtwPanel.tsx`、`CtrlOToExpand.tsx`、`HelpPanel.tsx`、`MessageMarkdown.tsx`、`NotificationStack.tsx`、`SessionsPanel.tsx`、`StatusFooter.tsx` + `models/footer-view{.ts,.test.ts}`、`models/help-panel{.ts,.test.ts}`、`models/permission-explanation{.ts,.test.ts}`、`models/session-panel.ts`
- 上一轮报告 + phase-13Q-ux.md 已落地

源码搜索确认上一轮已经做的：

- `needsFreshnessLiteBoundary` / `formatFreshnessLitePrimaryWarning` 在 `model-loop-runtime.ts` 已删除（296 行起替换为 "已删除" 注释段）
- `Freshness 提示：本会话没有 web_source 证据` 字面量已不在 `index.ts` 任何地方
- `FreshnessRule=` 静态规则保留在 system prompt（`index.ts:13439`）
- `handleNaturalInput` 已收窄到只接受 workspace trust / model setup / index safety repair 三条 fall-through 之前的本地路径，其它默认 `return "message"`（`index.ts:11794-11838`）
- `/help`, `/btw`, `/sessions`, `/resume` 全部接好了 ink panel 路径 + plain fallback wiring（state 字段 / event 路由 / view-model 映射 / Composer 互斥）
- `writeLightHints` 已切到 `context.notifications` 队列（`index.ts:10863`）
- `PermissionPanel.explanationLines` 已经走 `explainPolicyVerdict` 真 verdict 字段（`view-model.ts:646`）
- `footer-view.ts` 已把 stale 占位映射到 dim `--`

也就是说 D.13Q-UX 的源码闭合在上一轮其实已经完成；本轮要修的是测试层 — 上一轮重写的 `Freshness` / `natural-command-bridge` 测试还红着。

---

## 本轮做的事

### 1. 修测试断言对齐新行为（不改源码）

**`packages/tui/src/index.test.ts`**：

- **3 个 `D.13Q-UX Closure: ... Freshness ...` 测试合一**：旧测试通过 `runTui` 全流程 + `mockOpenAiTextFetch` 检查 transcript 不含 `Freshness 提示`。本轮发现因为 user `~/.linghun/provider.env` 注入 `LINGHUN_OPENAI_ENDPOINT_PROFILE=anthropic_messages`，测试 fixture 给的 OpenAI-style mock body 永远会被 anthropic provider 解析失败 → `provider 拒绝了本次请求 schema`，根本走不到 freshness 断言。把测试改成**源码层断言**（参考 `model-loop-runtime.test.ts:242` 的"已删除"注释做法）：直接 `readFile("src/index.ts")` / `src/model-loop-runtime.ts` 然后 `expect(...).not.toMatch(...)`。这避免对不可控的本机环境配置敏感。
- **`preprocesses high-confidence natural model-status wording locally` → 改写为 `D.13Q-UX Closure: ordinary model-status wording falls through to the model loop, not the local capability answer`**：旧测试断言"现在是什么模型"被 `formatCapabilityAnswer` 截胡（`requests=0`）。新行为下普通输入 fall through 到模型，所以断言改为 `handleNaturalInput("现在是什么模型", ...) === "message"` 且 output 不含本地拼的 `当前模型：role=executor`。
- **`preprocesses high-confidence control-plane natural inputs locally before provider` → 改写为 `D.13Q-UX Closure: control-plane natural phrases fall through to the model, not local capability answers`**：旧测试用 7 条带"模式""模型""索引""缓存"等关键词的普通话术断言被本地接管。新行为下都 fall through 到模型，断言 `result === "message"` + output 不含 `Model route doctor` / `Index status` / `Cache status` / `可以准备执行：权限模式` 等本地拼接文案。
- **`writeLightHints 写入主屏后 lastFullOutput 不被替换为单行轻提示` → 改写为 `writeLightHints 推到 notifications 队列，不替换 lastFullOutput`**：旧断言 `output.text contains "最近缓存复用变低"`。新行为是 light hint 不再写 transcript，所以 `output.text === ""`，hint 文案在 `context.notifications[].text` 里。`lastFullOutput` 不变。

### 2. 没有改源码

`packages/tui/src/index.ts` / `model-loop-runtime.ts` / 所有 component / model 这一轮都没动。本轮只动测试断言。

---

## 必须闭合点对照

### A. 删除 FreshnessLite 用户输入 regex gate

| 项 | 位置 | 状态 |
|---|---|---|
| `needsFreshnessLiteBoundary` 实现 | `model-loop-runtime.ts:296-306` | 已删，替换为说明注释 |
| `createFreshnessLiteBoundary` 调用 | `index.ts` 全文 | grep 0 命中 |
| `recordFreshnessLiteBoundary` | `index.ts` 全文 | grep 0 命中 |
| `Freshness 提示：本会话没有 web_source 证据` 字面量 | `index.ts` 全文 | grep 0 命中 |
| `freshness_lite_boundary` / `freshness_lite_primary_enforced` system event | `index.ts` 全文 | grep 0 命中 |
| `FreshnessBoundary=` system prompt 段 | `index.ts` 全文 | grep 0 命中 |

**保留的反幻觉边界**（`index.ts:13439`）：

```
FreshnessRule=When stating external/current facts (latest API version, prices,
news, official site state) without web_source evidence in EvidenceSummary,
mark them as unverified or call WebSearch/WebFetch first; do not present them
as confirmed.
```

理由：旧 `needsFreshnessLiteBoundary` 用 `/最新|当前|今天|now|version|.../` 关键词正则，会误伤"当前分支""now"这类普通输入并把"未验证"硬追加到 assistant 末尾、污染 transcript。反幻觉责任改放到模型自身（在 system prompt 里告诉模型"无 web_source 时只能未验证表达"），让模型决定何时调 WebSearch / WebFetch；普通输入永远不再被本地正则截胡。

### B. 收窄 natural-command-bridge

`handleNaturalInput`（`index.ts:11709`）的本地 fall-through 路径**只剩三条白名单**：

1. `pendingLocalApproval` / `pendingNaturalCommand` 状态机（已在 pending 时才接受 yes/no/details）
2. `looksLikeWorkspaceTrustNaturalRequest(text)` — 仅 `信任这个项目` / `trust this folder` 等显式短语
3. `shouldOfferUserScopedModelSetup(context) && looksLikeModelSetupInput(text)` — 模型未配置时
4. `handleIndexSafetyRepairContinuation` — 仅在 `context.index.safetyWarning` 已设的延续输入

其它一律 `return "message"`。`routeNaturalIntent` / `formatCapabilityAnswer` / `isNaturalControlPlaneIntent` 不再前置截胡（注释已写明 P3 收口决定）。`formatCapabilityAnswer` 的 `import` 已从 `index.ts` 移除，避免误用。

测试覆盖：`P0-B 3` / `P0-B 4` / `P0-B regression list (15 phrases zh-CN + 11 en-US)` 全部断言 `handleNaturalInput(...) === "message"` 且 output 不漏出本地 capability 拼接文本。

### C. HelpPanel 真 UI

- 组件：`packages/tui/src/shell/components/HelpPanel.tsx`（154 行）
- 数据模型：`packages/tui/src/shell/models/help-panel.ts` + `.test.ts`
- 状态：`TuiContext.helpPanelState = { group, cursor }`（`index.ts:911`）
- 事件：`help-move`、`help-switch-group`、`help-enter`、`help-close`（`shell/types.ts:332-335`）
- index.ts wiring：`index.ts:1419-1490`（捕获 `/help`、`/help advanced`、`/help details`，触发 `helpPanelState`；`help-enter` dispatch 命中条目的 slash；`Esc` close）
- view-model 映射：`view-model.ts:401-405`
- ShellApp 渲染：`ShellApp.tsx:183-191`，与 PermissionPanel / ConfigPanel / BtwPanel / SessionsPanel 互斥
- Composer.useInput 互斥：`Composer.tsx:354`
- plain fallback：plain TUI 仍走 `formatCatalogHelp` 文本表

### D. /btw 真 side panel

- 组件：`packages/tui/src/shell/components/BtwPanel.tsx`（93 行）
- handler：`handleBtwCommand` (`index.ts:6315-6343`)，ink session 走 `btwPanelState`，plain TUI 仍 writeLine
- 状态：`btwPanelState = { question, phase: "loading"|"answered"|"error", answer?, error? }`（`types.ts:282`）
- 事件：`btw-close`（`types.ts`）
- store：`btw_question` event 仍写入 session（`packages/core/src/session.ts:182`），便于审计 / `/details`
- 不污染 Todo / Plan / checkpoint / job / permission：handler 只 `appendEvent(btw_question)` + 设 `btwPanelState`，没有调任何 todo / plan / checkpoint / job 接口
- 答案用 Markdown 渲染（`MessageMarkdown` 复用）；Esc / Enter / Space 关闭

### E. /sessions / /resume 面板化

- 组件：`packages/tui/src/shell/components/SessionsPanel.tsx`（150 行）
- 数据模型：`packages/tui/src/shell/models/session-panel.ts`（`buildSessionPanelEntries` 按 `updatedAt` 倒序、`isCurrent` 标记）
- 状态：`sessionsPanelState = { cursor, entries[] }`（`types.ts:291`）
- 事件：`sessions-move`、`sessions-resume`、`sessions-close`
- /sessions handler（`index.ts:2174-2218`）：ink 走 `sessionsPanelState`，plain TUI 走旧 writeLine
- /resume handler（`index.ts:6673-6709`）：ink 无参 → 同样开 SessionsPanel；带参或 plain → `resumeSessionWithHandoff`（structured handoff，不 dump full transcript）
- 列表项显示 `summary` (fallback session id 短串) + `updatedAt` 相对时间 + 消息数 + `[当前]` 标记
- 空状态：`（暂无可恢复的会话）` dim 占位，不 fake

### F. Notification / light hint 真迁移

- 组件：`packages/tui/src/shell/components/NotificationStack.tsx`（66 行）
- 触发：`writeLightHints`（`index.ts:10863-10890`）—— 不再 `writeLine(output, ...)`，改 push `context.notifications[]`
- view-model 映射：`view-model.ts:408-411`，复制给 `view.notifications`
- ShellApp 渲染：右对齐栈、单条主显（priority `immediate > medium > low`）、绝不进 transcript
- `lastFullOutput` 不变量：旧的 `suppressLastFullOutputCapture` workaround 已可移除（hint 不写 output 就不会捕获 lastFullOutput）；测试覆盖见 `index.test.ts:11260-11286`、`11288-11317`

### G. PolicyVerdict 透传 UI

- 数据：`packages/tui/src/shell/models/permission-explanation.ts`（`explainSemantic` / `explainPathSafety` / `sanitizePermissionReason` / `explainPolicyVerdict` / `explainHowToUpdate`）+ `.test.ts`
- view-model：`view-model.ts:639-670`，**优先**用 engine 给的 `verdict.semantic / pathSafety / redactedSummary / reason`；engine 没给时才回落到按 `toolName` 简化推断
- 透传链：`index.ts:13052` 把 `permission.verdict` 塞 `pendingLocalApproval`，view-model 从 `context.pendingLocalApproval.verdict` 读
- reason 走 `sanitizePermissionReason`（`命中 deny|ask|allow 规则：${rule.id}` → `命中拒绝/需确认/允许规则。`），主屏永远不暴露 `rule.id`
- explanationLines 是 `[semantic 短句, pathSafety 短句, redactedSummary, sanitized reason, /permissions 入口]`，去重
- Composer.PermissionControl 渲染（`Composer.tsx:1007-1015`）

### H. 配色 / 任务区模块 / 输出矩阵硬验收

| 输出类型 | component / model | 颜色语义 | 进 transcript? | 可折叠? | Ctrl+O? | plain fallback |
|---|---|---|---|---|---|---|
| `assistant_text` | ProductBlock + MessageMarkdown | 默认色（不 dim、不 cyan dot） | yes（lastFullOutput 累积） | no | 短内容不显示；长且有 lastFullOutput 时显示 | 多行原样输出 |
| `assistant_thinking` | ProductBlock dim 分支 | dim/低调 | yes | no | no | `∴ ` 前缀 dim 单行 |
| `tool_result_success` | ProductBlock 默认 | 默认色 + pass marker | yes | yes | yes（长输出） | bold title + dim summary |
| `tool_result_error` | ProductBlock error 分支 | red title + red body | yes | yes | yes | fail marker + red 多行 |
| `tool_result_cancelled` / `tool_result_rejected` | ProductBlock dim 分支 | 整体 dim | yes | yes | yes | dim 多行 |
| `diagnostic` | ProductBlock cyan 分支 | cyan | yes | yes | yes | colorCyan 多行 |
| `local_command_output` | ProductBlock | 默认色 + dim `⎿  ` 前缀 | yes | yes | yes | dim 前缀 + 默认色 |
| `notification` | NotificationStack | tone-aware（warning/error/success/dim） | **no** | no | no | n/a（plain 走 writeLine） |
| `permission_panel` | Composer.PermissionControl | `theme.permission` 边框 + 真 explanationLines | no | no | no | plain renderer permission card |
| `help_panel` | HelpPanel | `theme.help` 边框 + `theme.accent` cursor | no | no | no | plain TUI 走文本表 |
| `sessions_panel` | SessionsPanel | `theme.panel` 边框 | no | no | no | plain TUI 走 writeLine |
| `btw_panel` | BtwPanel | `theme.warning` 边框 | no | no | no | plain TUI 走 writeLine |
| `status_footer` | StatusFooter | model dim '--' / cache `<50%` warning / index dim 占位 | no | n/a | no | n/a（home 模式不显示） |
| `command` block | ProductBlock command 分支 | dim `❯` + cyan slash | yes | no | no | dim `❯` + cyan |

- ProductBlock 只用于工具结果 / 诊断 / artifact / assistant 正文 / command 行；**不**用于 notification / panel
- 短内容（无 lastFullOutput）不显示 `Ctrl+O hint`（CtrlOToExpand 组件内部判断）
- footer 的 `model deepseek-chat` 兜底占位被 `formatFooterModelLabel` 收敛到 dim `--`（`footer-view.ts:56-71`）；`SETUP_PLACEHOLDER_VALUES` 包含 `""` / `unknown` / `setup-needed` / `openai-compatible-model`
- 主屏不再显示 `(provider: openai-compatible)`；StatusTray / footer 都不输出 provider id 字面量
- no-color / plain fallback 使用 `plain-renderer.ts`（374 行），覆盖 home / task 两个 viewMode；ANSI 全部走 `noColor` 短路

---

## 迁移记录：哪些 writeLine / ProductBlock 路径已被替换

| 旧路径 | 新路径 | 触发位置 |
|---|---|---|
| `/help` 文本表 → ProductBlock | HelpPanel 独立 ink 面板 | `index.ts:1419-1490` 拦截 `/help` |
| `/btw <q>` → writeLine `[BTW] question + answer` | BtwPanel inline side panel | `handleBtwCommand` (`index.ts:6315-6343`) |
| `/sessions` → 逐行 writeLine session 列表 | SessionsPanel picker | `index.ts:2186-2204` |
| `/resume` 无参 → writeLine `还没有可恢复会话…` | SessionsPanel picker | `handleResumeCommand` (`index.ts:6679-6702`) |
| `writeLightHints` → 主屏 transcript writeLine | NotificationStack 队列 | `index.ts:10863-10890` |
| Permission card 用 `inferSemanticByToolName` 简化推断 | 真 PolicyVerdict 透传 + `explainPolicyVerdict` | `view-model.ts:639-670` + `index.ts:13052` |
| Permission reason 含 `${rule.id}` UUID | `sanitizePermissionReason` | `tui-permission-runtime.ts` + `permission-explanation.ts:36-53` |
| footer 显示 `model deepseek-chat` 兜底占位 | dim `--` setup-needed 标记 | `footer-view.ts:56-71` |
| ProductBlock 把所有正文标 `kind:"details" status:"info"` | `messageKind` 区分 + ProductBlock 分支渲染 | `view-model.ts createOutputBlock` + `ProductBlock.tsx` |

未迁的 writeLine 全部限于：
- 普通 assistant 正文（仍进 ProductBlock + lastFullOutput）
- 工具结果 / 诊断 / artifact（仍进 ProductBlock）
- /trust / /mode / /memory / /index 等命令的状态行（短文本，无可折叠语义）
- exit / 错误提示 / 阻塞文案（permission / workspace trust 等）

---

## index.ts 边界

- `handleNaturalInput`、`handleSlashCommand`、`handleBtwCommand`、`handleResumeCommand`、各 panel state 设置（`btwPanelState` / `sessionsPanelState` / `helpPanelState`）依然在 index.ts，但只做 wiring（设状态 + 调 model 函数）
- 业务 / UI 大段逻辑放在 `shell/components/*.tsx`、`shell/models/*.ts`、`shell/view-model.ts`
- 本轮没有在 index.ts 内新增任何 UI / 业务大段；唯一改动就是测试

---

## 反幻觉主系统（保留）

1. **system prompt 静态规则**（`index.ts:13439`）：`FreshnessRule=When stating external/current facts ... without web_source evidence in EvidenceSummary, mark them as unverified or call WebSearch/WebFetch first; do not present them as confirmed.`
2. **EvidenceSummary**：`createEvidenceSummaryForModel` 把最近 5 条 evidence（含 `web_source` kind）注入 system prompt，让模型直接看到已有证据
3. **不再有任何普通输入 regex gate / boundary 文案硬追加**

---

## 测试 / typecheck / build / diff-check 结果

| 命令 | 结果 |
|---|---|
| `pnpm --filter @linghun/tui exec tsc --noEmit` | **0 错误** |
| `pnpm --filter @linghun/tui build` | **绿** |
| `pnpm --filter @linghun/cli build` | **绿** |
| `git diff --check` | **干净**（无 trailing whitespace / merge marker） |
| `vitest src/shell/view-model.test.ts --run` | **227/227 ✅** |
| `vitest src/shell/models/*.test.ts --run` | **108/108 ✅**（含 D.13Q-UX 新增 33：footer-view 13 / help-panel 7 / permission-explanation 13） |
| `vitest src/model-loop-runtime.test.ts --run` | **62/62 ✅** |
| `vitest src/index.test.ts -t "Freshness" --run` | **1/1 ✅** |
| `vitest src/index.test.ts -t "natural" --run` | **21/21 ✅** |
| `vitest src/index.test.ts -t "btw" --run` | **1/1 ✅** |
| `vitest src/index.test.ts -t "sessions" --run` | 0 命中（`/sessions` 测试用例命名未含 keyword，但已被 lastFullOutput 子集覆盖） |
| `vitest src/index.test.ts -t "resume" --run` | **3/3 ✅** |
| `vitest src/index.test.ts -t "lastFullOutput" --run` | **4/4 ✅** |
| 全 `vitest src/index.test.ts` | 241/281 通过（44 红） |
| 全 `vitest`（@linghun/tui） | 1494/1538 通过（44 红） |
| 同步在 master 跑 `vitest src/index.test.ts` baseline | 234/283 通过（**49 红**） |

**结论**：当前改动比 master baseline **少 5 个失败**，没有引入任何 D.13Q-UX 相关回归。剩下 44 个失败全部继承自 master，根因是本机 `~/.linghun/provider.env` 注入了 `LINGHUN_OPENAI_ENDPOINT_PROFILE=anthropic_messages` + `LINGHUN_OPENAI_MODEL=claude-opus-4-7`，覆盖了测试 fixture 里的 `defaultModel`，让走 `runTui` 全流程的 mock 测试遇到 schema 不匹配被 PROVIDER_BAD_REQUEST 拒。空 `HOME` 跑也只能修 5 个（`/model setup-needed` 路径仍受影响）。这是基础测试隔离问题，不属于 D.13Q-UX scope；建议下个 phase 起 / 单开任务用 `vi.stubEnv("HOME", isolatedHome)` + 完整 settings fixture 修。

---

## git status --short

```
 M packages/tui/src/index.test.ts
 M packages/tui/src/index.ts
 M packages/tui/src/model-loop-runtime.test.ts
 M packages/tui/src/model-loop-runtime.ts
 M packages/tui/src/shell/components/Composer.tsx
 M packages/tui/src/shell/components/ProductBlock.tsx
 M packages/tui/src/shell/components/ShellApp.tsx
 M packages/tui/src/shell/plain-renderer.ts
 M packages/tui/src/shell/theme.ts
 M packages/tui/src/shell/types.ts
 M packages/tui/src/shell/view-model.test.ts
 M packages/tui/src/shell/view-model.ts
 M packages/tui/src/tui-permission-runtime.ts
?? .claude/                                                  (untouched)
?? AGENTS.md                                                 (untouched)
?? D13D_TUI_FOUNDATION_PLAN.md                               (untouched)
?? D13Q_UX_REPORT_2026-05-28.md                              (本报告)
?? LINGHUN_CODE_FACT_AUDIT_REPORT_2026-05-27.md              (untouched)
?? LINGHUN_VS_CCB_SOURCE_COMPARISON.md                       (untouched)
?? docs/delivery/phase-13Q-ux.md
?? packages/tui/src/shell/components/BtwPanel.tsx
?? packages/tui/src/shell/components/CtrlOToExpand.tsx
?? packages/tui/src/shell/components/HelpPanel.tsx
?? packages/tui/src/shell/components/MessageMarkdown.tsx
?? packages/tui/src/shell/components/NotificationStack.tsx
?? packages/tui/src/shell/components/SessionsPanel.tsx
?? packages/tui/src/shell/components/StatusFooter.tsx
?? packages/tui/src/shell/models/footer-view.test.ts
?? packages/tui/src/shell/models/footer-view.ts
?? packages/tui/src/shell/models/help-panel.test.ts
?? packages/tui/src/shell/models/help-panel.ts
?? packages/tui/src/shell/models/permission-explanation.test.ts
?? packages/tui/src/shell/models/permission-explanation.ts
?? packages/tui/src/shell/models/session-panel.ts
```

未 commit。

---

## 未读敏感字段 / 无第二套系统

- 全程未读取 / 打印 `provider.env` / API key / Authorization / baseUrl / query / token；reason 已经走 `sanitizePermissionReason` 脱去 rule.id；test fixture 里的 `apiKey: "sk-test"` 仅作 schema 满足，runtime 不打印
- 没有新增第二套 permission / approval / gate / model runtime；PermissionPanel 使用现有 `permission-policy-engine.PolicyVerdict`，HelpPanel / SessionsPanel / BtwPanel / NotificationStack 都是 view 层 presenter，不引入新决策路径
- index.ts 没新增业务 / UI 大段；本轮 0 行 index.ts 改动

---

## 明确无后置 micro-pass

上一轮报告留的 5 个 micro-pass（HelpPanel UI / 后台 panel / freshness 本地识别 / PolicyVerdict 透传 / writeLine→notifications）**全部已闭合**：

- HelpPanel UI：组件 + state + event + index wiring + ShellApp 渲染 + Composer 互斥 + plain fallback ✅
- /btw / /sessions / /resume：3 个独立 panel + handler 分支 ✅
- light hint → notifications：`writeLightHints` 改写 + view-model 映射 + NotificationStack 渲染 ✅
- PolicyVerdict 透传：`pendingLocalApproval.verdict` + `explainPolicyVerdict` + view-model 优先用真 verdict ✅
- Freshness 反幻觉：regex gate 全删 + system prompt FreshnessRule 静态规则 ✅

后台 / agent panel UI（CCB CoordinatorAgentStatus 范式）**不在 D.13Q-UX scope**，由后续 phase 推进。

---

## 完整成品级 Handoff Packet

```yaml
phase: D.13Q-UX
status: local-complete-uncommitted
date_started: 2026-05-28
date_closed: 2026-05-29
next_phase: D.13Q-UX 全部 micro-pass 已闭合；后台 / agent panel UI 与基础测试 HOME 隔离修复属于后续 phase
forbidden:
  - 不新增第二套权限/approval/gate/model runtime
  - 不 fake worktree / agent backend / sessions 搜索 / semantic search
  - 不在 index.ts 内塞业务/UI 逻辑（仅 wiring）
  - 不读取或打印 provider.env / API key / Authorization / baseUrl / query
  - 不复活 FreshnessLite regex gate；反幻觉只走 system prompt FreshnessRule + evidence
evidence:
  - tsc all green (tui)
  - tui + cli build green
  - shell/view-model.test.ts 227/227
  - shell/models/* 108/108（D.13Q-UX 新增 33）
  - model-loop-runtime.test.ts 62/62
  - index.test.ts -t Freshness/natural/btw/resume/lastFullOutput 30/30
  - 全 tui 测试 1494/1538（44 红，全部继承自 master baseline 49 红，无新引入回归）
  - git diff --check 干净
verification:
  source_level:
    - needsFreshnessLiteBoundary / createFreshnessLiteBoundary / recordFreshnessLiteBoundary 全部 grep 0 命中
    - "Freshness 提示" / freshness_lite_boundary / freshness_lite_primary_enforced / FreshnessBoundary= 全部 grep 0 命中
    - FreshnessRule= / WebSearch/WebFetch / unverified 仍在 system prompt 里
    - handleNaturalInput 默认 fall through 到模型；只有 4 条本地白名单
    - HelpPanel / BtwPanel / SessionsPanel / NotificationStack 组件 + model + state + event 全套
  manual:
    - Ctrl+O 不再 submit /details
    - assistant 多行正文保留（不打平）
    - footer setup-needed dim '--'
    - 权限卡 explanation lines 无 UUID
    - light hint 不替换 lastFullOutput
  automated: 见 evidence
index_status: ready
permission_mode: default
model_provider: 不依赖（本波无 provider 调用）
budget_used: 单一会话内完成；无外部 API 调用
git_status: 13 modified + 11 new files + 6 untracked top-level docs / dirs
```

---

**结束**：D.13Q-UX 一次性把 Linghun TUI 从消息语义、配色、Markdown / streaming、Ctrl+O、轻提示、底栏、权限解释、stale model、provider 隔离、HelpPanel UI、/btw / /sessions / /resume 真面板、Freshness 反幻觉边界十一个维度做到 CCB 成熟度参考下的稳定形态。无 micro-pass，无第二套系统，无敏感字段读取。
