# Structural language fixtures

These fixtures cover syntax-level definitions, references, calls, and import or
module names across two files per language. They intentionally do not claim:

- Python dynamic imports, monkey patching, or runtime reflection;
- Go interface implementation or full module and type resolution;
- Rust macro expansion, crate features, or trait resolution;
- Java classpath, overload resolution, generated code, or dynamic dispatch.
